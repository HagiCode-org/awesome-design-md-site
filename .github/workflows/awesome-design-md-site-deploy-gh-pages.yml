name: Awesome Design Md Site Deploy gh-pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: awesome-design-md-site-deploy-gh-pages
  cancel-in-progress: false

permissions:
  contents: read

env:
  NODE_VERSION: "22.12.0"
  AWESOME_DESIGN_MD_SITE_GH_PAGES_PAYLOAD_DIR: .deploy/gh-pages

jobs:
  build:
    name: Build validated design gallery snapshot
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build and verify design gallery
        run: npm run build
        env:
          SITE_URL: https://design.hagicode.com

      - name: Assemble gh-pages publication payload
        run: |
          rm -rf "${AWESOME_DESIGN_MD_SITE_GH_PAGES_PAYLOAD_DIR}"
          mkdir -p "${AWESOME_DESIGN_MD_SITE_GH_PAGES_PAYLOAD_DIR}/dist"
          cp .github/gh-pages/esa.jsonc "${AWESOME_DESIGN_MD_SITE_GH_PAGES_PAYLOAD_DIR}/esa.jsonc"
          cp -R dist/. "${AWESOME_DESIGN_MD_SITE_GH_PAGES_PAYLOAD_DIR}/dist/"

      - name: Upload validated gh-pages payload
        uses: actions/upload-artifact@v4
        with:
          name: awesome-design-md-site-gh-pages-payload
          path: ${{ env.AWESOME_DESIGN_MD_SITE_GH_PAGES_PAYLOAD_DIR }}
          if-no-files-found: error

  deploy:
    name: Publish validated snapshot to gh-pages
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    environment:
      name: awesome-design-md-site-production
      url: https://design.hagicode.com
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download validated gh-pages payload
        uses: actions/download-artifact@v4
        with:
          name: awesome-design-md-site-gh-pages-payload
          path: .deploy/gh-pages

      - name: Publish validated gh-pages payload
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./.deploy/gh-pages
          force_orphan: true
          enable_jekyll: false

      - name: Summarize deployment output
        run: |
          echo "Published the validated gh-pages payload with ./esa.jsonc and ./dist/." >> "$GITHUB_STEP_SUMMARY"
          echo "The design gallery production branch is CI-owned and should not be edited manually." >> "$GITHUB_STEP_SUMMARY"
          echo "GitHub environment: awesome-design-md-site-production" >> "$GITHUB_STEP_SUMMARY"
          echo "Expected public URL: https://design.hagicode.com" >> "$GITHUB_STEP_SUMMARY"
