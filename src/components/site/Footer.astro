---
import type { NavItem } from '@/config/site';

interface Props {
  copy: string;
  links: NavItem[];
  metaLinks: NavItem[];
}

const { copy, links, metaLinks } = Astro.props;
---

<footer class="site-footer">
  <div class="container shell-panel footer-panel">
    <div class="footer-brand-column">
      <p class="footer-title">Awesome Design MD Gallery</p>
      <p class="footer-copy">{copy}</p>
      <div class="footer-meta-links">
        {metaLinks.map((item) => {
          const external = item.href.startsWith('http');

          return (
            <a
              class="footer-meta-link"
              href={item.href}
              target={external ? '_blank' : undefined}
              rel={external ? 'noreferrer' : undefined}
            >
              {item.label}
            </a>
          );
        })}
      </div>
    </div>
    <div class="footer-directory">
      <p class="footer-section-kicker">Related sites</p>
      <div class="footer-site-grid">
      {links.map((item) => {
        const external = item.href.startsWith('http');

        return (
          <a
            class="footer-site-card"
            href={item.href}
            target={external ? '_blank' : undefined}
            rel={external ? 'noreferrer' : undefined}
          >
            <span class="footer-link-title">{item.label}</span>
            {item.description ? <span class="footer-link-description">{item.description}</span> : null}
          </a>
        );
      })}
      </div>
    </div>
  </div>
</footer>
